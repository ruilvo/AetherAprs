<!--
This file is part of AetherAprs
SPDX-FileCopyrightText: 2026 Rui Oliveira <ruimail24@gmail.com>
SPDX-License-Identifier: GPL-3.0-or-later
-->
<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:AetherAprs.ViewModels.Pages"
             xmlns:messaging="using:AetherAprs.Models.Messaging"
             xmlns:local="using:AetherAprs"
             xmlns:wpf="clr-namespace:Material.Styles.Assists;assembly=Material.Styles"
             xmlns:loc="using:AetherAprs.Localization"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="800"
             x:Class="AetherAprs.Views.Pages.ConversationPage"
             x:DataType="vm:ConversationViewModel"
             d:DataContext="{x:Static local:DesignData.ConversationViewModel}">

  <Grid RowDefinitions="Auto,*,Auto,Auto">
    <!-- Header -->
    <StackPanel Grid.Row="0" Classes="PageHeader" Margin="16,16,16,0">
      <TextBlock Text="{Binding Title}" Classes="PageTitle" />

      <TextBox Text="{Binding DestinationCallsign}"
               IsEnabled="{Binding IsDestinationEditable}"
               PlaceholderText="{loc:Loc DestinationCallsignPlaceholder}"
               wpf:TextFieldAssist.Label="{loc:Loc To}"
               UseFloatingPlaceholder="True"
               Margin="0,8,0,0" />
    </StackPanel>

    <ScrollViewer Grid.Row="1" Margin="16">
      <ItemsControl ItemsSource="{Binding Messages}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel Spacing="8" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate x:DataType="messaging:StoredMessage">
            <Border Padding="8"
                    CornerRadius="8"
                    Background="{DynamicResource MaterialCardBackgroundBrush}">
              <StackPanel Spacing="4">
                <TextBlock Classes="Caption"
                           Foreground="{DynamicResource MaterialSecondaryBodyBrush}"
                           Text="{loc:Loc You}"
                           IsVisible="{Binding IsOutbound}" />
                <TextBlock Classes="Caption"
                           Foreground="{DynamicResource MaterialSecondaryBodyBrush}"
                           Text="{loc:Loc Received}"
                           IsVisible="{Binding !IsOutbound}" />
                <TextBlock Text="{Binding Text}" TextWrapping="Wrap" />
                <TextBlock Classes="Caption"
                           Foreground="{DynamicResource MaterialSecondaryBodyBrush}"
                           Text="{Binding DeliveryStatusText}"
                           IsVisible="{Binding DeliveryStatusText, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <TextBlock Grid.Row="2"
               Margin="16,0,16,8"
               Classes="Caption"
               Foreground="{DynamicResource MaterialValidationErrorBrush}"
               Text="{Binding ErrorMessage}"
               IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
               TextWrapping="Wrap" />

    <Border Grid.Row="3"
            Background="{DynamicResource MaterialBackgroundBrush}"
            BorderThickness="0,1,0,0"
            BorderBrush="{DynamicResource MaterialDividerBrush}"
            Padding="16">
      <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
        <TextBox Grid.Column="0"
                 Text="{Binding MessageText}"
                 PlaceholderText="{loc:Loc MessageTextPlaceholder}"
                 AcceptsReturn="False"
                 MaxLength="67" />
        <Button Grid.Column="1"
                Content="{loc:Loc Send}"
                MinWidth="80"
                MinHeight="48"
                Command="{Binding SendCommand}" />
      </Grid>
    </Border>
  </Grid>
</UserControl>
